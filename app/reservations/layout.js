export const metadata = {
  title: "Reserve a Table — Brew & Blossom | Pune",
  description:
    "Book a table at Brew & Blossom, Koregaon Park, Pune. Reservations recommended on weekends. Walk-ins welcome on weekdays.",
};

export default function ReservationsLayout({ children }) {
  return children;
}
